'use client';

export default function AdminLayout({ children }) {
  // This is just a pass-through layout for admin routes
  // Actual admin UI handling is done in nested layouts
  return children;
}
